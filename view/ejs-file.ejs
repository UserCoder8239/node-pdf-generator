<!DOCTYPE html>
<html>

<head>
    <style>
        html {
            zoom: 0.72;
        }

        body {
            overflow-x: hidden;
        }

        .top-title {
            font-size: 20px;
            width: 100%;
            text-align: center;
        }

        .card {
            flex-basis: 100%;
            border: 1px solid #dadada;
            border-radius: 4px;
            background-color: #fff;
            border-radius: 8px;
            width: 30%;
            float: left;
            font-size: 32px;
            text-align: center;
            box-sizing: border-box;
            margin-left: 1rem;
            margin-bottom: 0.3rem;
            margin-top: 1rem;
            vertical-align: middle;
            min-height: 130px;
            filter: drop-shadow(0px 4px 9px rgba(87, 87, 87, 0.08));
            background: #fdfbfb;
            border-radius: 8px;
            padding: 27px 24px;
            justify-content: space-between;
        }
    </style>
</head>

<body>
    <div class="top-title">
        Code Languages
    </div>
    <div class="container">

        <% data.forEach(function(value) { %>
            <div class="card">
                <span class="title" style="width: 75%; float: left; vertical-align: middle;">
                    <%= value %>
                </span>
            </div>
        <% }); %>
    </div>
</body>

</html>